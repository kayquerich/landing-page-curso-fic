const scrollButton = document.getElementById('scroll-button');

scrollButton.addEventListener('click', () => {
    window.scrollTo({
        top: 650,
        behavior: 'smooth'
    });
});

/* desenvolvendo a landing page não vi necessidade de JavaScript então fiz isso ai apenas como exemplo */